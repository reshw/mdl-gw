# TODO — 2026-04-13

## ✅ 완료

1. **주소록 개편 — 탭 + 검색 + 회사 필드 + 테이블 레이아웃**
   - 개인 주소록 / 전체 주소록 / MDL 멤버 3탭 구조로 전환
   - 이름·이메일·회사 기준 실시간 검색
   - 연락처에 회사(company) 필드 추가 — 추가·수정 모달 반영
   - 목록을 카드 대신 테이블 레이아웃으로 교체 (호버 시 수정/삭제 노출)

2. **MDL 멤버 통합**
   - Firestore `members/{email}` 컬렉션 신설
   - 가입 승인(`/api/admin/approve`) 시 members에 자동 저장
   - `getMdlMembers()` 함수 추가 → 주소록 멤버 탭에서 조회
   - `/api/admin/migrate-members` 라우트 추가 — 기존 승인 유저 일괄 마이그레이션
   - 어드민 페이지에 "멤버 마이그레이션" 버튼 추가

3. **Firestore rules 업데이트**
   - `members/{email}` — 읽기: 로그인 사용자 전체 / 쓰기: 서버(admin SDK)만
   - `contacts/{ownerEmail}/personal/{docId}` — 본인만 읽기·쓰기
   - `contacts/global/entries/{docId}` — 읽기: 전체 / 쓰기: 관리자만

4. **필터 버튼 → Funnel 아이콘 드롭다운**
   - 기존 "안읽음" / "라벨없음" 개별 버튼 제거
   - lucide-react `Funnel` 아이콘 버튼으로 통합
   - 드롭다운 내 체크박스로 각각 선택 — 둘 다 선택 시 교집합 필터 적용
   - 필터 활성 상태일 때 아이콘 버튼 강조(dark) 표시

5. **메일 목록 아이콘 — 읽음/안읽음 시각화 + 다용도 체크**
   - 안읽음: lucide `Mail` (민트색), 읽음: `MailOpen` (회색)
   - 호버 시 → `Square` (빈 체크박스) 로 전환
   - 클릭 시 → `CheckSquare` (체크 상태) 로 전환
   - 다른 항목 선택 중일 때는 전체 행이 Square 모드 유지

6. **다중 선택 읽음 처리**
   - 선택 메일 중 안읽은 것이 있으면 "읽음" 버튼 표시
   - 선택 메일 중 읽은 것이 있으면 "안읽음" 버튼 표시
   - 둘 다 섞여 있으면 두 버튼 동시 표시

7. **라벨 색상 — 커스텀 팔레트 추가**
   - 기존 7개 프리셋 색상 스와치 유지
   - 맨 끝에 무지개 그라디언트 원 버튼 추가 → 클릭 시 네이티브 컬러 피커 오픈
   - 선택한 hex 색상이 활성 스와치로 표시
   - `resolveLabelColor(color)` 헬퍼 추가 — hex / named 색상 모두 지원
   - 사이드바 도트, 메일 목록 도트, 뷰어 라벨 토글, 뷰어 라벨 pill 전체 반영
