# TODO — 2026-04-10

## ✅ 완료

1. **임시저장 기능 / 임시저장함** — 자동저장(30s), 수동저장, 임시저장함 폴더, 이어 작성
2. **메일 작성 — 표 삽입/편집기** — Jodit table 버튼 활성화, 셀 병합/분리, 행/열 추가·삭제
3. **CC / BCC** — 작성 모달 참조·숨은참조 필드, `/api/send` cc/bcc 연동, Resend API 전달
4. **다중 수신자 입력 — 칩(Chip) 스타일** — EmailChipInput, 받는사람·CC·BCC 적용
5. **답장 / 전달** — Reply/Forward 버튼, 원문 인용, Re:/Fwd: 자동 추가
6. **메일 삭제 / 휴지통** — 휴지통 이동, 영구 삭제, 복원
7. **메일 서명 + 설정 페이지 + 이미지 업로드**
   - 서명 설정 페이지 (`/settings`)
   - 작성 시 본문 하단 자동 삽입
   - Jodit 이미지 업로드 → Cloudflare R2 저장 → `/api/image` 서빙
8. **받은편지함 검색** _(본문 검색 제한 있음 — 숙제 참고)_
   - 제목 / 보낸사람 / 받는사람 / 본문(`text` 필드) 기준 클라이언트 사이드 실시간 필터
   - 폴더 전환 시 검색어 자동 초기화, 임시저장함도 검색 지원
   - ⚠️ **숙제**: HTML 에디터로 작성한 메일은 `text` 필드가 비어있어 본문 검색 불가
     → `/api/send`에서 발송 시 `html`을 평문으로 변환해 `text` 필드에 함께 저장해야 함
     → Firestore `mails` 문서에 `text` 값이 있어야 본문 검색 적중

---

## 진행 예정

### 9. 안 읽은 메일 뱃지 — 폴더별
- 현재 받은편지함만 표시, 임시저장함·휴지통도 카운트 표시

### 10. 대용량 메일 알림 개선
- 현재 10MB 초과 시 `failed: true` 플래그만 남김
- 실패 메일 본문에 원인 메시지 표시 + 재시도 안내

### 11. 주소록
- **개인 주소록**: 사용자별 연락처 (이름 + 이메일) — Firestore `contacts/{userEmail}/personal`
- **전체 주소록**: 관리자가 관리하는 공용 연락처 — Firestore `contacts/global/entries`
- 수신자 입력(EmailChipInput)에서 이름/이메일 자동완성 드롭다운
- 주소록 관리 UI: 사이드바에 진입점, 추가·수정·삭제
- 전체 주소록 편집은 관리자만 가능, 개인 주소록은 본인만

### 12. 메일 다중 선택 및 일괄 작업
- 메일 목록에서 체크박스로 다중 선택
- 선택한 메일 일괄 삭제 (휴지통 이동)
- 휴지통에서 선택한 메일 일괄 영구 삭제 / 일괄 복원
- 선택 상태 기준 액션 툴바 표시 (선택 N개 · 삭제 · 복원 · 전체선택)
- (확장) 선택한 수신자에게 단체 메일 발송 진입점

### 14. 안읽음으로 표시 (Mark as Unread)
- 메일 뷰어에 "안읽음으로 표시" 버튼 추가
- 다중 선택 툴바에도 일괄 안읽음 처리 버튼 추가 (받은편지함)
- `read: false` 로 되돌리되, 최초 열람 시각(`firstReadAt`)은 보존
  → 분쟁 발생 시 실제 열람 여부 확인 근거로 활용
- Firestore `mails/{id}` 에 `firstReadAt: ISO string` 필드 추가
  → 최초 `markAsRead` 시 한 번만 기록, 이후 안읽음 처리해도 변경되지 않음

### 13. 전체답장 (Reply All)
- 메일 뷰어에 "전체답장" 버튼 추가
- 받는사람(To): 원본 발송자
- 참조(CC): 원본 CC 수신자 전체 그대로 유지
- 제목: `Re:` 자동 추가, 원문 인용 포함
