# 2026-04-16 작업 기록

## 완료된 작업

### 1. notify.php (WP 서버 `/var/www/wordpress/notify.php`)
- PHPMailer + Naver SMTP (smtp.naver.com:587, STARTTLS) 로 구축
- `Encoding = 'base64'` 추가 → 본문 한글 깨짐 수정
- 발신자 이름: `메일알림`
- `mb_encode_mimeheader()` 추가 → 제목 한글 깨짐 수정 (미완 — 아래 참조)
- 인증: `secret` 필드로 NOTIFY_SECRET 검증

### 2. notify.ts (`src/lib/notify.ts`)
- 제목 포맷: `[OURIM] {메일제목}` (기존 `도착 내용 [OURIM]` → 간소화)
- Firestore `members/{email}.personalEmail` 조회 후 발송
- `notifications.emailEnabled === false` 이면 스킵 (opt-out 방식)

### 3. Cloudflare Worker (`mailer-worker/src/index.ts`)
- `ctx.waitUntil()` 추가 → fire-and-forget fetch가 핸들러 종료 전에 중단되는 문제 수정
- `--env ourim` 배포 완료

### 4. Vercel `ourim-gw` 프로젝트
- NOTIFY_ENDPOINT, NOTIFY_SECRET 환경변수 추가 후 재배포 완료

---

## ✅ 해결 완료 (2026-04-17)

### 원인
`mailer-ourim` Worker에 `NOTIFY_SECRET` secret이 등록되어 있지 않았음.
Worker 코드 내 `if (env.NOTIFY_SECRET)` 조건에서 항상 건너뜀 → `/api/notify` 호출 자체가 발생하지 않음.

### 해결
```bash
cd D:\dev\mailer-worker
echo "6db498c8..." | CLOUDFLARE_API_TOKEN=... npx wrangler secret put NOTIFY_SECRET --env ourim
```

### 삽질 기록 (반면교사)
- `D:\dev\mailer`에서 `npm run deploy --env ourim` 실행 시 **mailer-ourim Worker를 Next.js 앱으로 덮어씀** → 이메일 수신 처리 중단됨
  - 웹앱은 Vercel(gw.ourim.kr), mailer-ourim은 이메일 처리 전용 Worker로 분리되어있음
  - `D:\dev\mailer`의 deploy 명령은 사용 금지
- `mailer-worker/wrangler.toml` ourim env account_id가 `f333c...`(mdl용)으로 잘못 설정되어 있었음 → `139c...`(nayu.reshy, ourim용)으로 수정함
- `CLOUDFLARE_ACCOUNT_ID` 환경변수명 오기입(`CF_ACCOUNT_ID`로 넣으면 wrangler가 무시함)

---

## 주요 설정값

| 항목 | 값 |
|------|-----|
| NOTIFY_ENDPOINT | https://ourim.kr/notify.php |
| NOTIFY_SECRET | 6db498c8330f4b665f8e8789a44bdeeb7e7b0475304bf1f5 |
| sky@ourim.kr personalEmail | seokany@gmail.com |
| Naver SMTP | smtp.naver.com:587 / reshw@naver.com |
| WP 서버 SSH | ubuntu@158.179.165.88 (D:\dev_study\ourim\ssh-key-2026-04-02.key) |
| Worker env | ourim (`npx wrangler deploy --env ourim` in D:\dev\mailer-worker) |
| Vercel project | ourim-gw (gw.ourim.kr) |

---

## 파일별 현재 상태

| 파일 | 상태 |
|------|------|
| `/var/www/wordpress/notify.php` | 서버 배포 완료 (PHPMailer + Naver SMTP) |
| `src/lib/notify.ts` | Git push 완료 |
| `src/app/api/notify/route.ts` | Git push 완료 |
| `src/app/api/notifications/route.ts` | Git push 완료 |
| `src/app/api/send/route.ts` | notify() 호출 추가됨 |
| `src/app/settings/page.tsx` | 알림 탭 추가됨 |
| `mailer-worker/src/index.ts` | ctx.waitUntil 적용, ourim 배포 완료 |
